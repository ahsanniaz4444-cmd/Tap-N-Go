'use client';import {useEffect,useState} from 'react';import MobileShell from '../../components/MobileShell';import {api} from '../../lib/api';import {LineChart,Line,XAxis,YAxis,Tooltip,ResponsiveContainer} from 'recharts';
export default function Dashboard(){const [d,setD]=useState(null);useEffect(()=>{api('/api/dashboard').then(setD)},[]);if(!d)return <MobileShell title="Dashboard" subtitle="Loading..."/>;const trend=d.trend.map((v,i)=>({m:['Mon','Tue','Wed','Thu','Fri','Today'][i],v}));return <MobileShell title="Dashboard" subtitle="Retail overview"><div className="grid grid-cols-2 gap-3 mb-4">{[['Products',d.products],['Sales',`PKR ${d.totalSales}`],['Transactions',d.transactions],['Low Stock',d.lowStock]].map(([a,b])=><div className="card p-4" key={a}><p className="text-slate-500 text-xs font-bold">{a}</p><h2 className="text-2xl font-black mt-1">{b}</h2></div>)}</div><div className="card p-4 mb-4"><h3 className="font-black mb-3">Sales Trend</h3><ResponsiveContainer width="100%" height={220}><LineChart data={trend}><XAxis dataKey="m"/><YAxis/><Tooltip/><Line dataKey="v" strokeWidth={3}/></LineChart></ResponsiveContainer></div><div className="card p-4"><h3 className="font-black mb-3">Recent Sales</h3>{d.recentSales.map(s=><div className="flex justify-between py-2 border-b" key={s.id}><span>{s.id}<br/><small className="text-slate-500">{s.date}</small></span><b>PKR {s.total}</b></div>)}</div></MobileShell>}
