import {products} from '../../../lib/data'; export async function GET(){return Response.json(products)} export async function POST(req){const b=await req.json();const p={id:b.id||`P-${Date.now()}`,name:b.name,sku:b.sku||String(Date.now()),category:b.category||'General',price:Number(b.price||0),stock:Number(b.stock||0),minStock:Number(b.minStock||5)};products.unshift(p);return Response.json(p,{status:201})}
