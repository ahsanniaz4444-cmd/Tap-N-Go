'use client';import {useEffect,useState} from 'react';import MobileShell from '../../components/MobileShell';import {api} from '../../lib/api';
export default function Products(){const [rows,setRows]=useState([]);const [f,setF]=useState({name:'',sku:'',category:'Grocery',price:100,stock:10,minStock:5});useEffect(()=>{api('/api/products').then(setRows)},[]);async function add(e){e.preventDefault();const p=await api('/api/products',{method:'POST',body:JSON.stringify(f)});setRows([p,...rows]);setF({...f,name:'',sku:''})}return <MobileShell title="Products" subtitle="Catalog and inventory"><form onSubmit={add} className="card p-4 space-y-3 mb-4"><h3 className="font-black">Add Product</h3><input className="input" placeholder="Product name" value={f.name} onChange={e=>setF({...f,name:e.target.value})}/><input className="input" placeholder="Barcode / SKU" value={f.sku} onChange={e=>setF({...f,sku:e.target.value})}/><div className="grid grid-cols-2 gap-2"><input className="input" type="number" placeholder="Price" value={f.price} onChange={e=>setF({...f,price:e.target.value})}/><input className="input" type="number" placeholder="Stock" value={f.stock} onChange={e=>setF({...f,stock:e.target.value})}/></div><button className="btn btn-primary w-full">Save Product</button></form><div className="space-y-3">{rows.map(p=><div className="card p-4 flex justify-between" key={p.id}><div><b>{p.name}</b><p className="text-sm text-slate-500">{p.category} • SKU {p.sku}</p><span className={`badge ${p.stock<=p.minStock?'bg-red-50 text-red-700':'bg-green-50 text-green-700'}`}>Stock {p.stock}</span></div><b>PKR {p.price}</b></div>)}</div></MobileShell>}
