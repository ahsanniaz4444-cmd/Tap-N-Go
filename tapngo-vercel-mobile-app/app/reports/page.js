'use client';import {useEffect,useState} from 'react';import MobileShell from '../../components/MobileShell';import {api} from '../../lib/api';import {BarChart,Bar,XAxis,YAxis,Tooltip,ResponsiveContainer} from 'recharts';
export default function Reports(){const [r,setR]=useState(null);useEffect(()=>{api('/api/reports').then(setR)},[]);function csv(){const rows=[['Sale ID','Date','Total','Cashier'],...(r?.sales||[]).map(s=>[s.id,s.date,s.total,s.cashier])];const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([rows.map(x=>x.join(',')).join('\n')]));a.download='tapngo-sales-report.csv';a.click()}if(!r)return <MobileShell title="Reports" subtitle="Loading..."/>;return <MobileShell title="Reports" subtitle="Sales and inventory analytics"><button onClick={csv} className="btn btn-primary w-full mb-4">Export CSV Report</button><div className="card p-4 mb-4"><h3 className="font-black mb-3">Inventory by Category</h3><ResponsiveContainer width="100%" height={220}><BarChart data={r.summary.categories}><XAxis dataKey="category"/><YAxis/><Tooltip/><Bar dataKey="stock"/></BarChart></ResponsiveContainer></div><div className="card p-4"><h3 className="font-black mb-3">Inventory Report</h3><table className="table w-full"><thead><tr><th>Product</th><th>Stock</th><th>Status</th></tr></thead><tbody>{r.products.map(p=><tr key={p.id}><td>{p.name}</td><td>{p.stock}</td><td>{p.stock<=p.minStock?'Low':'OK'}</td></tr>)}</tbody></table></div></MobileShell>}
