'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';import {ShoppingBag} from 'lucide-react';import {api} from '../../lib/api';
export default function Login(){const [email,setEmail]=useState('admin@tapngo.com'),[password,setPassword]=useState('admin123'),[err,setErr]=useState('');const router=useRouter();async function submit(e){e.preventDefault();setErr('');try{const r=await api('/api/auth/login',{method:'POST',body:JSON.stringify({email,password})});localStorage.setItem('tapngo_user',JSON.stringify(r.user));router.push('/dashboard')}catch(x){setErr(x.message)}}return <main className="phone flex flex-col justify-center p-6 bg-gradient-to-br from-brand-900 to-brand-500"><div className="text-white mb-8"><div className="w-20 h-20 bg-white/20 rounded-[28px] grid place-items-center mb-4"><ShoppingBag size={42}/></div><h1 className="text-5xl font-black">Tap N Go</h1><p className="text-green-50 mt-2">Mobile POS, inventory, QR/barcode sale and business reports.</p></div><form onSubmit={submit} className="card p-5 space-y-4"><h2 className="text-2xl font-black">Sign in</h2><p className="text-sm text-slate-500">Demo: admin@tapngo.com / admin123</p>{err&&<p className="bg-red-50 text-red-700 p-3 rounded-2xl">{err}</p>}<input className="input" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email"/><input className="input" value={password} onChange={e=>setPassword(e.target.value)} type="password" placeholder="Password"/><button className="btn btn-primary w-full">Login</button></form></main>}
